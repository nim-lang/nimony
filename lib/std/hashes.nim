## Hashing lives in `system` now: the `Hash` type, `!&`/`!$`, `hash` for the
## builtin types, the `Hashable` concept and the `hashIgnoreCase`/`hashIgnoreStyle`
## helpers are all available without any import. This module remains so that
## `import std/hashes` keeps working.
