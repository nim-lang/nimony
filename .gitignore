bin/
bin0/
bin1/
bin2/
bin3/
nimcache/
nimblecache/
nimcache_static/
# tools/wine_test.sh: cross-built Windows toolchain, its nimcaches, and the
# MinGW bundle it links against (see the header of that script)
bin-wine/
nimcache_win/
nimcache_wine*/
external/
htmldocs/
src/hastur
src/lengc/lengc_grammar.nim
src/nifgram/nifgram
src/lib/nifindexes
tests/tester
tools/tester
*.exe
# per-directory driver hastur generates for a joined test group
_hastur_joined.nim
