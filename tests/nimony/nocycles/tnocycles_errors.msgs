tests/nimony/nocycles/tnocycles_errors.nim(18, 4) Error: assignment to field 'next' may create an ownership cycle. Hints: annotate field 'next' as .cursor if it is a back-reference (does not own); or use new() / a {.unique.} proc for a freshly allocated value; or add `requires: disjoint(...)` to the enclosing routine. See doc/nocycles.md. [pass --verbose for the NJ IR]
tests/nimony/nocycles/tnocycles_errors.nim(23, 8) Error: assignment to field 'next' may create an ownership cycle. Hints: annotate field 'next' as .cursor if it is a back-reference (does not own); or use new() / a {.unique.} proc for a freshly allocated value; or add `requires: disjoint(...)` to the enclosing routine. See doc/nocycles.md. [pass --verbose for the NJ IR]
tests/nimony/nocycles/tnocycles_errors.nim(28, 4) Error: assignment to field 'left' may create an ownership cycle. Hints: annotate field 'left' as .cursor if it is a back-reference (does not own); or use new() / a {.unique.} proc for a freshly allocated value; or add `requires: disjoint(...)` to the enclosing routine. See doc/nocycles.md. [pass --verbose for the NJ IR]
