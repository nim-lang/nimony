/* Fixture header for tcbindingsplugin2.nim.
 *
 * The bindings plugin emits `header: "foo.h"` pragmas and the C backend
 * includes this file. Keep the fixture empty for now; the plugin test only
 * checks that generated declarations survive the full pipeline.
 */
