{
  "name": "nimony-jsbackend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only tooling for the JS backend tests: a DOM environment (jsdom, for tdom) and the WebIDL binding generator (webidl2 + @webref/idl, for gen/idl2nim.js). Run `npm install` here to enable them; node_modules is git-ignored, not vendored.",
  "license": "MIT",
  "devDependencies": {
    "@webref/idl": "^3.81.2",
    "jsdom": "^29.1.1",
    "webidl2": "^24.5.0"
  }
}
