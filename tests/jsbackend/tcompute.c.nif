(.nif27)
(stmts
 (proc :add.0.tcompute
  (params
   (param :a.0 . (i 32))
   (param :b.0 . (i 32)))
  (i 32)
  .
  (stmts
   (ret (add (i 32) a.0 b.0))))
 (proc :fib.0.tcompute
  (params
   (param :n.0 . (i 32)))
  (i 32)
  .
  (stmts
   (if
    (elif (lt n.0 2)
     (stmts (ret n.0))))
   (ret (add (i 32)
    (call fib.0.tcompute (sub (i 32) n.0 1))
    (call fib.0.tcompute (sub (i 32) n.0 2)))))))
