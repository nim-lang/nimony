(.nif27)
(stmts
 (proc :dbl.0.tfnptr
  (params (param :n.0 . (i 32)))
  (i 32)
  .
  (stmts (ret (mul (i 32) n.0 2))))
 (proc :callit.0.tfnptr
  (params)
  (i 32)
  .
  (stmts
   (var :f.0 . (ptr (i 32)) dbl.0.tfnptr)
   (ret (call f.0 21)))))
