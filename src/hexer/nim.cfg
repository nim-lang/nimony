--path: "../lib"
# Btw this relies on a Nim bug where it interprets the --outdir relative to the current directory
# instead of the directory of this nim.cfg file.
--outdir: "bin"
# hexer runs on the in-memory virtual-ParRi token model
# (sealed jump fields + ParRi elision), like lengc and nimsem:
--define: virtualParRi
