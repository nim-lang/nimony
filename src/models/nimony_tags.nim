# Generated by tools/gen_tags.nim from doc/tags.md. DO NOT EDIT!


type
  NimonyExpr* = enum
    NoExpr
    ErrX = (1, "err")  ## indicates an error
    SufX = (2, "suf")  ## literal with suffix annotation
    AtX = (3, "at")  ## array indexing operation
    DerefX = (4, "deref")  ## pointer deref operation
    DotX = (5, "dot")  ## object field selection
    PatX = (6, "pat")  ## pointer indexing operation
    ParX = (7, "par")  ## syntactic parenthesis
    AddrX = (8, "addr")  ## address of operation
    NilX = (9, "nil")  ## nil pointer value
    InfX = (10, "inf")  ## positive infinity floating point value
    NeginfX = (11, "neginf")  ## negative infinity floating point value
    NanX = (12, "nan")  ## NaN floating point value
    FalseX = (13, "false")  ## boolean `false` value
    TrueX = (14, "true")  ## boolean `true` value
    AndX = (15, "and")  ## boolean `and` operation
    OrX = (16, "or")  ## boolean `or` operation
    NotX = (17, "not")  ## boolean `not` operation
    NegX = (18, "neg")  ## negation operation
    SizeofX = (19, "sizeof")  ## `sizeof` operation
    AlignofX = (20, "alignof")  ## `alignof` operation
    OffsetofX = (21, "offsetof")  ## `offsetof` operation
    OconstrX = (22, "oconstr")  ## object constructor
    AconstrX = (23, "aconstr")  ## array constructor
    BracketX = (24, "bracket")  ## untyped array constructor
    CurlyX = (25, "curly")  ## untyped set constructor
    AddX = (27, "add")
    SubX = (28, "sub")
    MulX = (29, "mul")
    DivX = (30, "div")
    ModX = (31, "mod")
    ShrX = (32, "shr")
    ShlX = (33, "shl")
    BitandX = (34, "bitand")
    BitorX = (35, "bitor")
    BitxorX = (36, "bitxor")
    BitnotX = (37, "bitnot")
    EqX = (38, "eq")
    NeqX = (39, "neq")
    LeX = (40, "le")
    LtX = (41, "lt")
    CastX = (42, "cast")
    ConvX = (43, "conv")  ## type conversion
    CallX = (44, "call")  ## call operation
    CmdX = (45, "cmd")  ## command operation
    CchoiceX = (69, "cchoice")  ## closed choice
    OchoiceX = (70, "ochoice")  ## open choice
    QuotedX = (197, "quoted")  ## name in backticks
    HderefX = (198, "hderef")  ## hidden pointer deref operation
    DdotX = (199, "ddot")  ## deref dot
    HaddrX = (200, "haddr")  ## hidden address of operation
    NewoconstrX = (201, "newoconstr")  ## new object constructor
    TupleconstrX = (202, "tupleconstr")  ## tuple constructor
    SetX = (203, "set")  ## set constructor
    AshrX = (204, "ashr")
    OconvX = (205, "oconv")  ## object conversion
    HconvX = (206, "hconv")  ## hidden basic type conversion
    DconvX = (207, "dconv")  ## conversion between `distinct` types
    CallstrlitX = (208, "callstrlit")
    InfixX = (209, "infix")
    PrefixX = (210, "prefix")
    HcallX = (211, "hcall")  ## hidden converter call
    CompilesX = (212, "compiles")
    DeclaredX = (213, "declared")
    DefinedX = (214, "defined")
    HighX = (215, "high")
    LowX = (216, "low")
    TypeofX = (217, "typeof")
    UnpackX = (218, "unpack")
    EnumtostrX = (219, "enumtostr")
    IsmainmoduleX = (220, "ismainmodule")
    DefaultobjX = (221, "defaultobj")
    DefaulttupX = (222, "defaulttup")
    ExprX = (223, "expr")
    ArratX = (224, "arrat")
    TupatX = (225, "tupat")
    PlussetX = (226, "plusset")
    MinussetX = (227, "minusset")
    MulsetX = (228, "mulset")
    XorsetX = (229, "xorset")
    EqsetX = (230, "eqset")
    LesetX = (231, "leset")
    LtsetX = (232, "ltset")
    InsetX = (233, "inset")
    CardX = (234, "card")
    EmoveX = (235, "emove")
    DestroyX = (236, "destroy")
    DupX = (237, "dup")
    CopyX = (238, "copy")
    WasmovedX = (239, "wasmoved")
    SinkhX = (240, "sinkh")
    TraceX = (241, "trace")

proc rawTagIsNimonyExpr*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {1'u8, 2'u8, 3'u8, 4'u8, 5'u8, 6'u8, 7'u8, 8'u8, 9'u8, 10'u8, 11'u8, 12'u8, 13'u8, 14'u8, 15'u8, 16'u8, 17'u8, 18'u8, 19'u8, 20'u8, 21'u8, 22'u8, 23'u8, 24'u8, 25'u8, 27'u8, 28'u8, 29'u8, 30'u8, 31'u8, 32'u8, 33'u8, 34'u8, 35'u8, 36'u8, 37'u8, 38'u8, 39'u8, 40'u8, 41'u8, 42'u8, 43'u8, 44'u8, 45'u8, 69'u8, 70'u8, 197'u8, 198'u8, 199'u8, 200'u8, 201'u8, 202'u8, 203'u8, 204'u8, 205'u8, 206'u8, 207'u8, 208'u8, 209'u8, 210'u8, 211'u8, 212'u8, 213'u8, 214'u8, 215'u8, 216'u8, 217'u8, 218'u8, 219'u8, 220'u8, 221'u8, 222'u8, 223'u8, 224'u8, 225'u8, 226'u8, 227'u8, 228'u8, 229'u8, 230'u8, 231'u8, 232'u8, 233'u8, 234'u8, 235'u8, 236'u8, 237'u8, 238'u8, 239'u8, 240'u8, 241'u8}

type
  NimonyStmt* = enum
    NoStmt
    CallS = (44, "call")  ## call operation
    CmdS = (45, "cmd")  ## command operation
    VarS = (50, "var")  ## variable declaration
    ConstS = (52, "const")  ## const variable declaration
    ResultS = (53, "result")  ## result variable declaration
    LetS = (54, "let")  ## let variable declaration
    CursorS = (55, "cursor")  ## cursor variable declaration
    ProcS = (59, "proc")  ## proc declaration
    FuncS = (60, "func")  ## function declaration
    IteratorS = (61, "iterator")  ## iterator declaration
    ConverterS = (62, "converter")  ## converter declaration
    MethodS = (63, "method")  ## method declaration
    MacroS = (64, "macro")  ## macro declaration
    TemplateS = (65, "template")  ## template declaration
    TypeS = (66, "type")  ## type declaration
    BlockS = (67, "block")  ## block declaration
    EmitS = (71, "emit")  ## emit statement
    AsgnS = (72, "asgn")  ## assignment statement
    ScopeS = (73, "scope")  ## explicit scope annotation, like `stmts`
    IfS = (74, "if")  ## if statement header
    WhenS = (75, "when")  ## when statement header
    BreakS = (79, "break")  ## `break` statement
    ContinueS = (80, "continue")  ## `continue` statement
    ForS = (81, "for")  ## for statement
    WhileS = (82, "while")  ## `while` statement
    CaseS = (83, "case")  ## `case` statement
    RetS = (87, "ret")  ## `return` instruction
    YldS = (88, "yld")  ## yield statement
    StmtsS = (89, "stmts")  ## list of statements
    PragmasS = (122, "pragmas")  ## begin of pragma section
    InclS = (128, "incl")  ## `#include` statement or `incl` set operation
    ExclS = (129, "excl")  ## `excl` set operation
    IncludeS = (130, "include")  ## `include` statement
    ImportS = (131, "import")  ## `import` statement
    FromS = (132, "from")  ## `from` statement
    ImportexceptS = (133, "importexcept")  ## `importexcept` statement
    ExportS = (134, "export")  ## `export` statement
    CommentS = (135, "comment")  ## `comment` statement
    DiscardS = (136, "discard")  ## `discard` statement
    TryS = (137, "try")  ## `try` statement
    RaiseS = (138, "raise")  ## `raise` statement

proc rawTagIsNimonyStmt*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {44'u8, 45'u8, 50'u8, 52'u8, 53'u8, 54'u8, 55'u8, 59'u8, 60'u8, 61'u8, 62'u8, 63'u8, 64'u8, 65'u8, 66'u8, 67'u8, 71'u8, 72'u8, 73'u8, 74'u8, 75'u8, 79'u8, 80'u8, 81'u8, 82'u8, 83'u8, 87'u8, 88'u8, 89'u8, 122'u8, 128'u8, 129'u8, 130'u8, 131'u8, 132'u8, 133'u8, 134'u8, 135'u8, 136'u8, 137'u8, 138'u8}

type
  NimonyType* = enum
    NoType
    ErrT = (1, "err")  ## indicates an error
    AtT = (3, "at")  ## array indexing operation
    AndT = (15, "and")  ## boolean `and` operation
    OrT = (16, "or")  ## boolean `or` operation
    NotT = (17, "not")  ## boolean `not` operation
    IteratorT = (61, "iterator")  ## iterator declaration
    ParamsT = (90, "params")  ## list of proc parameters, also used as a "proc type"
    ObjectT = (92, "object")  ## object type declaration
    EnumT = (93, "enum")  ## enum type declaration
    ProctypeT = (94, "proctype")  ## proc type declaration (soon obsolete, use params instead)
    IT = (98, "i")  ## `int` builtin type
    UT = (99, "u")  ## `uint` builtin type
    FT = (100, "f")  ## `float` builtin type
    CT = (101, "c")  ## `char` builtin type
    BoolT = (102, "bool")  ## `bool` builtin type
    VoidT = (103, "void")  ## `void` return type
    PtrT = (104, "ptr")  ## `ptr` type contructor
    ArrayT = (105, "array")  ## `array` type constructor
    VarargsT = (119, "varargs")  ## `varargs` proc annotation
    StaticT = (142, "static")  ## `static` type or annotation
    RefobjT = (151, "refobj")  ## `ref object` type
    PtrobjT = (152, "ptrobj")  ## `ptr object` type
    TupleT = (153, "tuple")  ## `tuple` type
    OnumT = (154, "onum")  ## enum with holes type
    RefT = (155, "ref")  ## `ref` type
    MutT = (156, "mut")  ## `mut` type
    OutT = (157, "out")  ## `out` type
    LentT = (158, "lent")  ## `lent` type
    SinkT = (159, "sink")  ## `sink` type
    NiltT = (160, "nilt")  ## `nilt` type
    ConceptT = (161, "concept")  ## `concept` type
    DistinctT = (162, "distinct")  ## `distinct` type
    ItertypeT = (163, "itertype")  ## `itertype` type
    RangetypeT = (164, "rangetype")  ## `rangetype` type
    UarrayT = (165, "uarray")  ## `uarray` type
    OpenarrayT = (166, "openarray")  ## `openarray` type
    SettT = (167, "sett")  ## `sett` type
    AutoT = (168, "auto")  ## `auto` type
    SymkindT = (169, "symkind")  ## `symkind` type
    TypekindT = (170, "typekind")  ## `typekind` type
    TypedescT = (171, "typedesc")  ## `typedesc` type
    UntypedT = (172, "untyped")  ## `untyped` type
    TypedT = (173, "typed")  ## `typed` type
    CstringT = (174, "cstring")  ## `cstring` type
    PointerT = (175, "pointer")  ## `pointer` type
    OrdinalT = (176, "ordinal")  ## `ordinal` type

proc rawTagIsNimonyType*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {1'u8, 3'u8, 15'u8, 16'u8, 17'u8, 61'u8, 90'u8, 92'u8, 93'u8, 94'u8, 98'u8, 99'u8, 100'u8, 101'u8, 102'u8, 103'u8, 104'u8, 105'u8, 119'u8, 142'u8, 151'u8, 152'u8, 153'u8, 154'u8, 155'u8, 156'u8, 157'u8, 158'u8, 159'u8, 160'u8, 161'u8, 162'u8, 163'u8, 164'u8, 165'u8, 166'u8, 167'u8, 168'u8, 169'u8, 170'u8, 171'u8, 172'u8, 173'u8, 174'u8, 175'u8, 176'u8}

type
  NimonyOther* = enum
    NoSub
    KvU = (26, "kv")  ## key-value pair
    RangeU = (46, "range")  ## `(range a b)` construct
    RangesU = (47, "ranges")
    ParamU = (51, "param")  ## parameter declaration
    TypevarU = (56, "typevar")  ## type variable declaration
    EfldU = (57, "efld")  ## enum field declaration
    FldU = (58, "fld")  ## field declaration
    ElifU = (76, "elif")  ## pair of (condition, action)
    ElseU = (77, "else")  ## `else` action
    TypevarsU = (78, "typevars")  ## type variable/generic parameters
    OfU = (84, "of")  ## `of` branch within a `case` statement
    PragmasU = (122, "pragmas")  ## begin of pragma section
    UnpackflatU = (147, "unpackflat")  ## unpack into flat variable list
    UnpacktupU = (148, "unpacktup")  ## unpack tuple
    ExceptU = (149, "except")  ## except subsection
    FinU = (150, "fin")  ## finally subsection

proc rawTagIsNimonyOther*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {26'u8, 46'u8, 47'u8, 51'u8, 56'u8, 57'u8, 58'u8, 76'u8, 77'u8, 78'u8, 84'u8, 122'u8, 147'u8, 148'u8, 149'u8, 150'u8}

type
  NimonyPragma* = enum
    NoPragma
    EmitP = (71, "emit")  ## emit statement
    InlineP = (116, "inline")  ## `inline` proc annotation
    NoinlineP = (117, "noinline")  ## `noinline` proc annotation
    VarargsP = (119, "varargs")  ## `varargs` proc annotation
    SelectanyP = (121, "selectany")
    AlignP = (123, "align")
    BitsP = (124, "bits")
    NodeclP = (127, "nodecl")  ## `nodecl` annotation
    RaisesP = (140, "raises")  ## proc annotation
    MagicP = (177, "magic")  ## `magic` pragma
    ImportcP = (178, "importc")  ## `importc` pragma
    ImportcppP = (179, "importcpp")  ## `importcpp` pragma
    ExportcP = (180, "exportc")  ## `exportc` pragma
    HeaderP = (181, "header")  ## `header` pragma
    ThreadvarP = (182, "threadvar")  ## `threadvar` pragma
    GlobalP = (183, "global")  ## `global` pragma
    DiscardableP = (184, "discardable")  ## `discardable` pragma
    NoreturnP = (185, "noreturn")  ## `noreturn` pragma
    BorrowP = (186, "borrow")  ## `borrow` pragma
    NoSideEffectP = (187, "noSideEffect")  ## `noSideEffect` pragma
    NodestroyP = (188, "nodestroy")  ## `nodestroy` pragma
    PluginP = (189, "plugin")  ## `plugin` pragma
    BycopyP = (190, "bycopy")  ## `bycopy` pragma
    ByrefP = (191, "byref")  ## `byref` pragma
    NoinitP = (192, "noinit")  ## `noinit` pragma
    RequiresP = (193, "requires")  ## `requires` pragma
    EnsuresP = (194, "ensures")  ## `ensures` pragma
    BuildP = (195, "build")  ## `build` pragma
    StringP = (196, "string")  ## `string` pragma

proc rawTagIsNimonyPragma*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {71'u8, 116'u8, 117'u8, 119'u8, 121'u8, 123'u8, 124'u8, 127'u8, 140'u8, 177'u8, 178'u8, 179'u8, 180'u8, 181'u8, 182'u8, 183'u8, 184'u8, 185'u8, 186'u8, 187'u8, 188'u8, 189'u8, 190'u8, 191'u8, 192'u8, 193'u8, 194'u8, 195'u8, 196'u8}

type
  NimonySym* = enum
    NoSym
    VarY = (50, "var")  ## variable declaration
    ParamY = (51, "param")  ## parameter declaration
    ConstY = (52, "const")  ## const variable declaration
    ResultY = (53, "result")  ## result variable declaration
    LetY = (54, "let")  ## let variable declaration
    CursorY = (55, "cursor")  ## cursor variable declaration
    TypevarY = (56, "typevar")  ## type variable declaration
    EfldY = (57, "efld")  ## enum field declaration
    FldY = (58, "fld")  ## field declaration
    ProcY = (59, "proc")  ## proc declaration
    FuncY = (60, "func")  ## function declaration
    IteratorY = (61, "iterator")  ## iterator declaration
    ConverterY = (62, "converter")  ## converter declaration
    MethodY = (63, "method")  ## method declaration
    MacroY = (64, "macro")  ## macro declaration
    TemplateY = (65, "template")  ## template declaration
    TypeY = (66, "type")  ## type declaration
    BlockY = (67, "block")  ## block declaration
    ModuleY = (68, "module")  ## module declaration
    CchoiceY = (69, "cchoice")  ## closed choice

proc rawTagIsNimonySym*(raw: uint32): bool =
  raw >= 50'u32 and raw <= 69'u32

type
  ControlFlowKind* = enum
    NoControlFlow
    IteF = (143, "ite")  ## if-then-else
    GraphF = (144, "graph")  ## disjoint subgraph annotation
    ForbindF = (145, "forbind")  ## bindings for a `for` loop but the loop itself is mapped to gotos
    KillF = (146, "kill")  ## some.var is about to disappear (scope exit)

proc rawTagIsControlFlowKind*(raw: uint32): bool =
  raw >= 143'u32 and raw <= 146'u32

