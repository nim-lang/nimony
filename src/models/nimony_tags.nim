# Generated by tools/gen_tags.nim from doc/tags.md. DO NOT EDIT!


type
  NimonyExpr* = enum
    NoExpr
    ErrX = (1, "err")  ## indicates an error
    SufX = (2, "suf")  ## literal with suffix annotation
    AtX = (3, "at")  ## array indexing operation
    DerefX = (4, "deref")  ## pointer deref operation
    DotX = (5, "dot")  ## object field selection
    PatX = (6, "pat")  ## pointer indexing operation
    ParX = (7, "par")  ## syntactic parenthesis
    AddrX = (8, "addr")  ## address of operation
    NilX = (9, "nil")  ## nil pointer value
    InfX = (10, "inf")  ## positive infinity floating point value
    NeginfX = (11, "neginf")  ## negative infinity floating point value
    NanX = (12, "nan")  ## NaN floating point value
    FalseX = (13, "false")  ## boolean `false` value
    TrueX = (14, "true")  ## boolean `true` value
    AndX = (15, "and")  ## boolean `and` operation
    OrX = (16, "or")  ## boolean `or` operation
    NotX = (17, "not")  ## boolean `not` operation
    NegX = (18, "neg")  ## negation operation
    SizeofX = (19, "sizeof")  ## `sizeof` operation
    AlignofX = (20, "alignof")  ## `alignof` operation
    OffsetofX = (21, "offsetof")  ## `offsetof` operation
    ObjX = (23, "obj")  ## object constructor
    ArrX = (25, "arr")  ## array constructor
    BracketX = (26, "bracket")  ## untyped array constructor
    CurlyX = (27, "curly")  ## untyped set constructor
    AddX = (29, "add")
    SubX = (30, "sub")
    MulX = (31, "mul")
    DivX = (32, "div")
    ModX = (33, "mod")
    ShrX = (34, "shr")
    ShlX = (35, "shl")
    BitandX = (36, "bitand")
    BitorX = (37, "bitor")
    BitxorX = (38, "bitxor")
    BitnotX = (39, "bitnot")
    EqX = (40, "eq")
    NeqX = (41, "neq")
    LeX = (42, "le")
    LtX = (43, "lt")
    CastX = (44, "cast")
    ConvX = (45, "conv")  ## type conversion
    CallX = (46, "call")  ## call operation
    CmdX = (47, "cmd")  ## command operation
    CchoiceX = (71, "cchoice")  ## closed choice
    OchoiceX = (72, "ochoice")  ## open choice
    QuotedX = (201, "quoted")  ## name in backticks
    HderefX = (202, "hderef")  ## hidden pointer deref operation
    DdotX = (203, "ddot")  ## deref dot
    HaddrX = (204, "haddr")  ## hidden address of operation
    NewobjX = (205, "newobj")  ## new object constructor
    TupX = (206, "tup")  ## tuple constructor
    SetconstrX = (207, "setconstr")  ## set constructor
    AshrX = (208, "ashr")
    OconvX = (209, "oconv")  ## object conversion
    HconvX = (210, "hconv")  ## hidden basic type conversion
    DconvX = (211, "dconv")  ## conversion between `distinct` types
    CallstrlitX = (212, "callstrlit")
    InfixX = (213, "infix")
    PrefixX = (214, "prefix")
    HcallX = (215, "hcall")  ## hidden converter call
    CompilesX = (216, "compiles")
    DeclaredX = (217, "declared")
    DefinedX = (218, "defined")
    HighX = (219, "high")
    LowX = (220, "low")
    TypeofX = (221, "typeof")
    UnpackX = (222, "unpack")
    EnumtostrX = (223, "enumtostr")
    IsmainmoduleX = (224, "ismainmodule")
    DefaultobjX = (225, "defaultobj")
    DefaulttupX = (226, "defaulttup")
    ExprX = (227, "expr")
    ArratX = (228, "arrat")
    TupatX = (229, "tupat")
    PlussetX = (230, "plusset")
    MinussetX = (231, "minusset")
    MulsetX = (232, "mulset")
    XorsetX = (233, "xorset")
    EqsetX = (234, "eqset")
    LesetX = (235, "leset")
    LtsetX = (236, "ltset")
    InsetX = (237, "inset")
    CardX = (238, "card")
    EmoveX = (239, "emove")
    DestroyX = (240, "destroy")
    DupX = (241, "dup")
    CopyX = (242, "copy")
    WasmovedX = (243, "wasmoved")
    SinkhX = (244, "sinkh")
    TraceX = (245, "trace")

proc rawTagIsNimonyExpr*(raw: uint32): bool {.inline.} =
  raw <= 255'u32 and raw.uint8 in {1'u8, 2'u8, 3'u8, 4'u8, 5'u8, 6'u8, 7'u8, 8'u8, 9'u8, 10'u8, 11'u8, 12'u8, 13'u8, 14'u8, 15'u8, 16'u8, 17'u8, 18'u8, 19'u8, 20'u8, 21'u8, 23'u8, 25'u8, 26'u8, 27'u8, 29'u8, 30'u8, 31'u8, 32'u8, 33'u8, 34'u8, 35'u8, 36'u8, 37'u8, 38'u8, 39'u8, 40'u8, 41'u8, 42'u8, 43'u8, 44'u8, 45'u8, 46'u8, 47'u8, 71'u8, 72'u8, 201'u8, 202'u8, 203'u8, 204'u8, 205'u8, 206'u8, 207'u8, 208'u8, 209'u8, 210'u8, 211'u8, 212'u8, 213'u8, 214'u8, 215'u8, 216'u8, 217'u8, 218'u8, 219'u8, 220'u8, 221'u8, 222'u8, 223'u8, 224'u8, 225'u8, 226'u8, 227'u8, 228'u8, 229'u8, 230'u8, 231'u8, 232'u8, 233'u8, 234'u8, 235'u8, 236'u8, 237'u8, 238'u8, 239'u8, 240'u8, 241'u8, 242'u8, 243'u8, 244'u8, 245'u8}

type
  NimonyStmt* = enum
    NoStmt
    CallS = (46, "call")  ## call operation
    CmdS = (47, "cmd")  ## command operation
    VarS = (52, "var")  ## variable declaration
    ConstS = (54, "const")  ## const variable declaration
    ResultS = (55, "result")  ## result variable declaration
    LetS = (56, "let")  ## let variable declaration
    CursorS = (57, "cursor")  ## cursor variable declaration
    ProcS = (61, "proc")  ## proc declaration
    FuncS = (62, "func")  ## function declaration
    IteratorS = (63, "iterator")  ## iterator declaration
    ConverterS = (64, "converter")  ## converter declaration
    MethodS = (65, "method")  ## method declaration
    MacroS = (66, "macro")  ## macro declaration
    TemplateS = (67, "template")  ## template declaration
    TypeS = (68, "type")  ## type declaration
    BlockS = (69, "block")  ## block declaration
    EmitS = (73, "emit")  ## emit statement
    AsgnS = (74, "asgn")  ## assignment statement
    ScopeS = (75, "scope")  ## explicit scope annotation, like `stmts`
    IfS = (76, "if")  ## if statement header
    WhenS = (77, "when")  ## when statement header
    BreakS = (81, "break")  ## `break` statement
    ContinueS = (82, "continue")  ## `continue` statement
    ForS = (83, "for")  ## for statement
    WhileS = (84, "while")  ## `while` statement
    CaseS = (85, "case")  ## `case` statement
    RetS = (89, "ret")  ## `return` instruction
    YldS = (90, "yld")  ## yield statement
    StmtsS = (91, "stmts")  ## list of statements
    PragmasS = (126, "pragmas")  ## begin of pragma section
    InclS = (132, "incl")  ## `#include` statement or `incl` set operation
    ExclS = (133, "excl")  ## `excl` set operation
    IncludeS = (134, "include")  ## `include` statement
    ImportS = (135, "import")  ## `import` statement
    FromS = (136, "from")  ## `from` statement
    ImportexceptS = (137, "importexcept")  ## `importexcept` statement
    ExportS = (138, "export")  ## `export` statement
    CommentS = (139, "comment")  ## `comment` statement
    DiscardS = (140, "discard")  ## `discard` statement
    TryS = (141, "try")  ## `try` statement
    RaiseS = (142, "raise")  ## `raise` statement

proc rawTagIsNimonyStmt*(raw: uint32): bool {.inline.} =
  raw <= 255'u32 and raw.uint8 in {46'u8, 47'u8, 52'u8, 54'u8, 55'u8, 56'u8, 57'u8, 61'u8, 62'u8, 63'u8, 64'u8, 65'u8, 66'u8, 67'u8, 68'u8, 69'u8, 73'u8, 74'u8, 75'u8, 76'u8, 77'u8, 81'u8, 82'u8, 83'u8, 84'u8, 85'u8, 89'u8, 90'u8, 91'u8, 126'u8, 132'u8, 133'u8, 134'u8, 135'u8, 136'u8, 137'u8, 138'u8, 139'u8, 140'u8, 141'u8, 142'u8}

type
  NimonyType* = enum
    NoType
    ErrT = (1, "err")  ## indicates an error
    AtT = (3, "at")  ## array indexing operation
    AndT = (15, "and")  ## boolean `and` operation
    OrT = (16, "or")  ## boolean `or` operation
    NotT = (17, "not")  ## boolean `not` operation
    IteratorT = (63, "iterator")  ## iterator declaration
    ParamsT = (92, "params")  ## list of proc parameters, also used as a "proc type"
    ObjectT = (94, "object")  ## object type declaration
    EnumT = (95, "enum")  ## enum type declaration
    ProctypeT = (96, "proctype")  ## proc type declaration (soon obsolete, use params instead)
    IT = (101, "i")  ## `int` builtin type
    UT = (102, "u")  ## `uint` builtin type
    FT = (103, "f")  ## `float` builtin type
    CT = (104, "c")  ## `char` builtin type
    BoolT = (105, "bool")  ## `bool` builtin type
    VoidT = (106, "void")  ## `void` return type
    PtrT = (107, "ptr")  ## `ptr` type contructor
    ArrayT = (108, "array")  ## `array` type constructor
    VarargsT = (123, "varargs")  ## `varargs` proc annotation
    StaticT = (146, "static")  ## `static` type or annotation
    RefobjT = (155, "refobj")  ## `ref object` type
    PtrobjT = (156, "ptrobj")  ## `ptr object` type
    TupleT = (157, "tuple")  ## `tuple` type
    OnumT = (158, "onum")  ## enum with holes type
    RefT = (159, "ref")  ## `ref` type
    MutT = (160, "mut")  ## `mut` type
    OutT = (161, "out")  ## `out` type
    LentT = (162, "lent")  ## `lent` type
    SinkT = (163, "sink")  ## `sink` type
    NiltT = (164, "nilt")  ## `nilt` type
    ConceptT = (165, "concept")  ## `concept` type
    DistinctT = (166, "distinct")  ## `distinct` type
    ItertypeT = (167, "itertype")  ## `itertype` type
    RangetypeT = (168, "rangetype")  ## `rangetype` type
    UarrayT = (169, "uarray")  ## `uarray` type
    OpenarrayT = (170, "openarray")  ## `openarray` type
    SetT = (171, "set")  ## `set` type
    AutoT = (172, "auto")  ## `auto` type
    SymkindT = (173, "symkind")  ## `symkind` type
    TypekindT = (174, "typekind")  ## `typekind` type
    TypedescT = (175, "typedesc")  ## `typedesc` type
    UntypedT = (176, "untyped")  ## `untyped` type
    TypedT = (177, "typed")  ## `typed` type
    CstringT = (178, "cstring")  ## `cstring` type
    PointerT = (179, "pointer")  ## `pointer` type
    OrdinalT = (180, "ordinal")  ## `ordinal` type

proc rawTagIsNimonyType*(raw: uint32): bool {.inline.} =
  raw <= 255'u32 and raw.uint8 in {1'u8, 3'u8, 15'u8, 16'u8, 17'u8, 63'u8, 92'u8, 94'u8, 95'u8, 96'u8, 101'u8, 102'u8, 103'u8, 104'u8, 105'u8, 106'u8, 107'u8, 108'u8, 123'u8, 146'u8, 155'u8, 156'u8, 157'u8, 158'u8, 159'u8, 160'u8, 161'u8, 162'u8, 163'u8, 164'u8, 165'u8, 166'u8, 167'u8, 168'u8, 169'u8, 170'u8, 171'u8, 172'u8, 173'u8, 174'u8, 175'u8, 176'u8, 177'u8, 178'u8, 179'u8, 180'u8}

type
  NimonyOther* = enum
    NoSub
    KvU = (28, "kv")  ## key-value pair
    RangeU = (48, "range")  ## `(range a b)` construct
    RangesU = (49, "ranges")
    ParamU = (53, "param")  ## parameter declaration
    TypevarU = (58, "typevar")  ## type variable declaration
    EfldU = (59, "efld")  ## enum field declaration
    FldU = (60, "fld")  ## field declaration
    ElifU = (78, "elif")  ## pair of (condition, action)
    ElseU = (79, "else")  ## `else` action
    TypevarsU = (80, "typevars")  ## type variable/generic parameters
    OfU = (86, "of")  ## `of` branch within a `case` statement
    PragmasU = (126, "pragmas")  ## begin of pragma section
    UnpackflatU = (151, "unpackflat")  ## unpack into flat variable list
    UnpacktupU = (152, "unpacktup")  ## unpack tuple
    ExceptU = (153, "except")  ## except subsection
    FinU = (154, "fin")  ## finally subsection

proc rawTagIsNimonyOther*(raw: uint32): bool {.inline.} =
  raw <= 255'u32 and raw.uint8 in {28'u8, 48'u8, 49'u8, 53'u8, 58'u8, 59'u8, 60'u8, 78'u8, 79'u8, 80'u8, 86'u8, 126'u8, 151'u8, 152'u8, 153'u8, 154'u8}

type
  NimonyPragma* = enum
    NoPragma
    EmitP = (73, "emit")  ## emit statement
    InlineP = (120, "inline")  ## `inline` proc annotation
    NoinlineP = (121, "noinline")  ## `noinline` proc annotation
    VarargsP = (123, "varargs")  ## `varargs` proc annotation
    SelectanyP = (125, "selectany")
    AlignP = (127, "align")
    BitsP = (128, "bits")
    NodeclP = (131, "nodecl")  ## `nodecl` annotation
    RaisesP = (144, "raises")  ## proc annotation
    MagicP = (181, "magic")  ## `magic` pragma
    ImportcP = (182, "importc")  ## `importc` pragma
    ImportcppP = (183, "importcpp")  ## `importcpp` pragma
    ExportcP = (184, "exportc")  ## `exportc` pragma
    HeaderP = (185, "header")  ## `header` pragma
    ThreadvarP = (186, "threadvar")  ## `threadvar` pragma
    GlobalP = (187, "global")  ## `global` pragma
    DiscardableP = (188, "discardable")  ## `discardable` pragma
    NoreturnP = (189, "noreturn")  ## `noreturn` pragma
    BorrowP = (190, "borrow")  ## `borrow` pragma
    NoSideEffectP = (191, "noSideEffect")  ## `noSideEffect` pragma
    NodestroyP = (192, "nodestroy")  ## `nodestroy` pragma
    PluginP = (193, "plugin")  ## `plugin` pragma
    BycopyP = (194, "bycopy")  ## `bycopy` pragma
    ByrefP = (195, "byref")  ## `byref` pragma
    NoinitP = (196, "noinit")  ## `noinit` pragma
    RequiresP = (197, "requires")  ## `requires` pragma
    EnsuresP = (198, "ensures")  ## `ensures` pragma
    BuildP = (199, "build")  ## `build` pragma
    StringP = (200, "string")  ## `string` pragma

proc rawTagIsNimonyPragma*(raw: uint32): bool {.inline.} =
  raw <= 255'u32 and raw.uint8 in {73'u8, 120'u8, 121'u8, 123'u8, 125'u8, 127'u8, 128'u8, 131'u8, 144'u8, 181'u8, 182'u8, 183'u8, 184'u8, 185'u8, 186'u8, 187'u8, 188'u8, 189'u8, 190'u8, 191'u8, 192'u8, 193'u8, 194'u8, 195'u8, 196'u8, 197'u8, 198'u8, 199'u8, 200'u8}

type
  NimonySym* = enum
    NoSym
    VarY = (52, "var")  ## variable declaration
    ParamY = (53, "param")  ## parameter declaration
    ConstY = (54, "const")  ## const variable declaration
    ResultY = (55, "result")  ## result variable declaration
    LetY = (56, "let")  ## let variable declaration
    CursorY = (57, "cursor")  ## cursor variable declaration
    TypevarY = (58, "typevar")  ## type variable declaration
    EfldY = (59, "efld")  ## enum field declaration
    FldY = (60, "fld")  ## field declaration
    ProcY = (61, "proc")  ## proc declaration
    FuncY = (62, "func")  ## function declaration
    IteratorY = (63, "iterator")  ## iterator declaration
    ConverterY = (64, "converter")  ## converter declaration
    MethodY = (65, "method")  ## method declaration
    MacroY = (66, "macro")  ## macro declaration
    TemplateY = (67, "template")  ## template declaration
    TypeY = (68, "type")  ## type declaration
    BlockY = (69, "block")  ## block declaration
    ModuleY = (70, "module")  ## module declaration
    CchoiceY = (71, "cchoice")  ## closed choice

proc rawTagIsNimonySym*(raw: uint32): bool {.inline.} =
  raw >= 52'u32 and raw <= 71'u32

type
  ControlFlowKind* = enum
    NoControlFlow
    IteF = (147, "ite")  ## if-then-else
    GraphF = (148, "graph")  ## disjoint subgraph annotation
    ForbindF = (149, "forbind")  ## bindings for a `for` loop but the loop itself is mapped to gotos
    KillF = (150, "kill")  ## some.var is about to disappear (scope exit)

proc rawTagIsControlFlowKind*(raw: uint32): bool {.inline.} =
  raw >= 147'u32 and raw <= 150'u32

