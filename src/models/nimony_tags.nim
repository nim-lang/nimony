# Generated by tools/gen_tags.nim from doc/tags.md. DO NOT EDIT!


type
  NimonyExpr* = enum
    NoExpr
    ErrX = (1, "err")  ## indicates an error
    SufX = (2, "suf")  ## literal with suffix annotation
    AtX = (3, "at")  ## array indexing operation
    DerefX = (4, "deref")  ## pointer deref operation
    DotX = (5, "dot")  ## object field selection
    PatX = (6, "pat")  ## pointer indexing operation
    ParX = (7, "par")  ## syntactic parenthesis
    AddrX = (8, "addr")  ## address of operation
    NilX = (9, "nil")  ## nil pointer value
    InfX = (10, "inf")  ## positive infinity floating point value
    NeginfX = (11, "neginf")  ## negative infinity floating point value
    NanX = (12, "nan")  ## NaN floating point value
    FalseX = (13, "false")  ## boolean `false` value
    TrueX = (14, "true")  ## boolean `true` value
    AndX = (15, "and")  ## boolean `and` operation
    OrX = (16, "or")  ## boolean `or` operation
    NotX = (17, "not")  ## boolean `not` operation
    NegX = (18, "neg")  ## negation operation
    SizeofX = (19, "sizeof")  ## `sizeof` operation
    AlignofX = (20, "alignof")  ## `alignof` operation
    OffsetofX = (21, "offsetof")  ## `offsetof` operation
    OconstrX = (22, "oconstr")  ## object constructor
    AconstrX = (23, "aconstr")  ## array constructor
    BracketX = (24, "bracket")  ## untyped array constructor
    CurlyX = (25, "curly")  ## untyped set constructor
    CurlyatX = (26, "curlyat")  ## curly expression `a{i}`
    AddX = (29, "add")
    SubX = (30, "sub")
    MulX = (31, "mul")
    DivX = (32, "div")
    ModX = (33, "mod")
    ShrX = (34, "shr")
    ShlX = (35, "shl")
    BitandX = (36, "bitand")
    BitorX = (37, "bitor")
    BitxorX = (38, "bitxor")
    BitnotX = (39, "bitnot")
    EqX = (40, "eq")
    NeqX = (41, "neq")
    LeX = (42, "le")
    LtX = (43, "lt")
    CastX = (44, "cast")  ## `cast` operation
    ConvX = (45, "conv")  ## type conversion
    CallX = (46, "call")  ## call operation
    CmdX = (47, "cmd")  ## command operation
    CchoiceX = (71, "cchoice")  ## closed choice
    OchoiceX = (72, "ochoice")  ## open choice
    PragmaxX = (127, "pragmax")  ## pragma expressions
    QuotedX = (205, "quoted")  ## name in backticks
    HderefX = (206, "hderef")  ## hidden pointer deref operation
    DdotX = (207, "ddot")  ## deref dot
    HaddrX = (208, "haddr")  ## hidden address of operation
    NewobjX = (209, "newobj")  ## new object constructor
    TupX = (210, "tup")  ## tuple constructor
    SetconstrX = (211, "setconstr")  ## set constructor
    TabconstrX = (212, "tabconstr")  ## table constructor
    AshrX = (213, "ashr")
    OconvX = (214, "oconv")  ## object conversion
    HconvX = (215, "hconv")  ## hidden basic type conversion
    DconvX = (216, "dconv")  ## conversion between `distinct` types
    CallstrlitX = (217, "callstrlit")
    InfixX = (218, "infix")
    PrefixX = (219, "prefix")
    HcallX = (220, "hcall")  ## hidden converter call
    CompilesX = (221, "compiles")
    DeclaredX = (222, "declared")
    DefinedX = (223, "defined")
    HighX = (224, "high")
    LowX = (225, "low")
    TypeofX = (226, "typeof")
    UnpackX = (227, "unpack")
    EnumtostrX = (228, "enumtostr")
    IsmainmoduleX = (229, "ismainmodule")
    DefaultobjX = (230, "defaultobj")
    DefaulttupX = (231, "defaulttup")
    ExprX = (232, "expr")
    DoX = (233, "do")  ## `do` expression
    ArratX = (234, "arrat")
    TupatX = (235, "tupat")
    PlussetX = (236, "plusset")
    MinussetX = (237, "minusset")
    MulsetX = (238, "mulset")
    XorsetX = (239, "xorset")
    EqsetX = (240, "eqset")
    LesetX = (241, "leset")
    LtsetX = (242, "ltset")
    InsetX = (243, "inset")
    CardX = (244, "card")
    EmoveX = (245, "emove")
    DestroyX = (246, "destroy")
    DupX = (247, "dup")
    CopyX = (248, "copy")
    WasmovedX = (249, "wasmoved")
    SinkhX = (250, "sinkh")
    TraceX = (251, "trace")

proc rawTagIsNimonyExpr*(raw: uint32): bool {.inline.} =
  let r = raw
  r <= 255'u32 and r.uint8 in {1'u8, 2'u8, 3'u8, 4'u8, 5'u8, 6'u8, 7'u8, 8'u8, 9'u8, 10'u8, 11'u8, 12'u8, 13'u8, 14'u8, 15'u8, 16'u8, 17'u8, 18'u8, 19'u8, 20'u8, 21'u8, 22'u8, 23'u8, 24'u8, 25'u8, 26'u8, 29'u8, 30'u8, 31'u8, 32'u8, 33'u8, 34'u8, 35'u8, 36'u8, 37'u8, 38'u8, 39'u8, 40'u8, 41'u8, 42'u8, 43'u8, 44'u8, 45'u8, 46'u8, 47'u8, 71'u8, 72'u8, 127'u8, 205'u8, 206'u8, 207'u8, 208'u8, 209'u8, 210'u8, 211'u8, 212'u8, 213'u8, 214'u8, 215'u8, 216'u8, 217'u8, 218'u8, 219'u8, 220'u8, 221'u8, 222'u8, 223'u8, 224'u8, 225'u8, 226'u8, 227'u8, 228'u8, 229'u8, 230'u8, 231'u8, 232'u8, 233'u8, 234'u8, 235'u8, 236'u8, 237'u8, 238'u8, 239'u8, 240'u8, 241'u8, 242'u8, 243'u8, 244'u8, 245'u8, 246'u8, 247'u8, 248'u8, 249'u8, 250'u8, 251'u8}

type
  NimonyStmt* = enum
    NoStmt
    CallS = (46, "call")  ## call operation
    CmdS = (47, "cmd")  ## command operation
    VarS = (52, "var")  ## variable declaration
    ConstS = (54, "const")  ## const variable declaration
    ResultS = (55, "result")  ## result variable declaration
    LetS = (56, "let")  ## let variable declaration
    CursorS = (57, "cursor")  ## cursor variable declaration
    ProcS = (61, "proc")  ## proc declaration
    FuncS = (62, "func")  ## function declaration
    IteratorS = (63, "iterator")  ## iterator declaration
    ConverterS = (64, "converter")  ## converter declaration
    MethodS = (65, "method")  ## method declaration
    MacroS = (66, "macro")  ## macro declaration
    TemplateS = (67, "template")  ## template declaration
    TypeS = (68, "type")  ## type declaration
    BlockS = (69, "block")  ## block declaration
    EmitS = (73, "emit")  ## emit statement
    AsgnS = (74, "asgn")  ## assignment statement
    ScopeS = (75, "scope")  ## explicit scope annotation, like `stmts`
    IfS = (76, "if")  ## if statement header
    WhenS = (77, "when")  ## when statement header
    BreakS = (81, "break")  ## `break` statement
    ContinueS = (82, "continue")  ## `continue` statement
    ForS = (83, "for")  ## for statement
    WhileS = (84, "while")  ## `while` statement
    CaseS = (85, "case")  ## `case` statement
    RetS = (89, "ret")  ## `return` instruction
    YldS = (90, "yld")  ## yield statement
    StmtsS = (91, "stmts")  ## list of statements
    PragmasS = (126, "pragmas")  ## begin of pragma section
    InclS = (133, "incl")  ## `#include` statement or `incl` set operation
    ExclS = (134, "excl")  ## `excl` set operation
    IncludeS = (135, "include")  ## `include` statement
    ImportS = (136, "import")  ## `import` statement
    ImportasS = (137, "importas")  ## `import as` statement
    FromS = (138, "from")  ## `from` statement
    ImportexceptS = (139, "importexcept")  ## `importexcept` statement
    ExportS = (140, "export")  ## `export` statement
    ExportexceptS = (141, "exportexcept")  ## `exportexcept` statement
    CommentS = (142, "comment")  ## `comment` statement
    DiscardS = (143, "discard")  ## `discard` statement
    TryS = (144, "try")  ## `try` statement
    RaiseS = (145, "raise")  ## `raise` statement
    UnpackdeclS = (156, "unpackdecl")  ## unpack var/let/const declaration
    StaticstmtS = (253, "staticstmt")  ## `static` statement
    BindS = (254, "bind")  ## `bind` statement
    MixinS = (255, "mixin")  ## `mixin` statement
    UsingS = (256, "using")  ## `using` statement
    AsmS = (257, "asm")  ## `asm` statement
    DeferS = (258, "defer")  ## `defer` statement

proc rawTagIsNimonyStmt*(raw: uint32): bool {.inline.} =
  let r = raw - 46'u32
  r <= 255'u32 and r.uint8 in {0'u8, 1'u8, 6'u8, 8'u8, 9'u8, 10'u8, 11'u8, 15'u8, 16'u8, 17'u8, 18'u8, 19'u8, 20'u8, 21'u8, 22'u8, 23'u8, 27'u8, 28'u8, 29'u8, 30'u8, 31'u8, 35'u8, 36'u8, 37'u8, 38'u8, 39'u8, 43'u8, 44'u8, 45'u8, 80'u8, 87'u8, 88'u8, 89'u8, 90'u8, 91'u8, 92'u8, 93'u8, 94'u8, 95'u8, 96'u8, 97'u8, 98'u8, 99'u8, 110'u8, 207'u8, 208'u8, 209'u8, 210'u8, 211'u8, 212'u8}

type
  NimonyType* = enum
    NoType
    ErrT = (1, "err")  ## indicates an error
    AtT = (3, "at")  ## array indexing operation
    AndT = (15, "and")  ## boolean `and` operation
    OrT = (16, "or")  ## boolean `or` operation
    NotT = (17, "not")  ## boolean `not` operation
    IteratorT = (63, "iterator")  ## iterator declaration
    ParamsT = (92, "params")  ## list of proc parameters, also used as a "proc type"
    ObjectT = (94, "object")  ## object type declaration
    EnumT = (95, "enum")  ## enum type declaration
    ProctypeT = (96, "proctype")  ## proc type declaration (soon obsolete, use params instead)
    IT = (101, "i")  ## `int` builtin type
    UT = (102, "u")  ## `uint` builtin type
    FT = (103, "f")  ## `float` builtin type
    CT = (104, "c")  ## `char` builtin type
    BoolT = (105, "bool")  ## `bool` builtin type
    VoidT = (106, "void")  ## `void` return type
    PtrT = (107, "ptr")  ## `ptr` type contructor
    ArrayT = (108, "array")  ## `array` type constructor
    VarargsT = (123, "varargs")  ## `varargs` proc annotation
    StaticT = (149, "static")  ## `static` type or annotation
    RefobjT = (159, "refobj")  ## `ref object` type
    PtrobjT = (160, "ptrobj")  ## `ptr object` type
    TupleT = (161, "tuple")  ## `tuple` type
    OnumT = (162, "onum")  ## enum with holes type
    RefT = (163, "ref")  ## `ref` type
    MutT = (164, "mut")  ## `mut` type
    OutT = (165, "out")  ## `out` type
    LentT = (166, "lent")  ## `lent` type
    SinkT = (167, "sink")  ## `sink` type
    NiltT = (168, "nilt")  ## `nilt` type
    ConceptT = (169, "concept")  ## `concept` type
    DistinctT = (170, "distinct")  ## `distinct` type
    ItertypeT = (171, "itertype")  ## `itertype` type
    RangetypeT = (172, "rangetype")  ## `rangetype` type
    UarrayT = (173, "uarray")  ## `uarray` type
    OpenarrayT = (174, "openarray")  ## `openarray` type
    SetT = (175, "set")  ## `set` type
    AutoT = (176, "auto")  ## `auto` type
    SymkindT = (177, "symkind")  ## `symkind` type
    TypekindT = (178, "typekind")  ## `typekind` type
    TypedescT = (179, "typedesc")  ## `typedesc` type
    UntypedT = (180, "untyped")  ## `untyped` type
    TypedT = (181, "typed")  ## `typed` type
    CstringT = (182, "cstring")  ## `cstring` type
    PointerT = (183, "pointer")  ## `pointer` type
    OrdinalT = (184, "ordinal")  ## `ordinal` type

proc rawTagIsNimonyType*(raw: uint32): bool {.inline.} =
  let r = raw
  r <= 255'u32 and r.uint8 in {1'u8, 3'u8, 15'u8, 16'u8, 17'u8, 63'u8, 92'u8, 94'u8, 95'u8, 96'u8, 101'u8, 102'u8, 103'u8, 104'u8, 105'u8, 106'u8, 107'u8, 108'u8, 123'u8, 149'u8, 159'u8, 160'u8, 161'u8, 162'u8, 163'u8, 164'u8, 165'u8, 166'u8, 167'u8, 168'u8, 169'u8, 170'u8, 171'u8, 172'u8, 173'u8, 174'u8, 175'u8, 176'u8, 177'u8, 178'u8, 179'u8, 180'u8, 181'u8, 182'u8, 183'u8, 184'u8}

type
  NimonyOther* = enum
    NoSub
    KvU = (27, "kv")  ## key-value pair
    VvU = (28, "vv")  ## value-value pair (used for explicitly named arguments in function calls)
    RangeU = (48, "range")  ## `(range a b)` construct
    RangesU = (49, "ranges")
    ParamU = (53, "param")  ## parameter declaration
    TypevarU = (58, "typevar")  ## type variable declaration
    EfldU = (59, "efld")  ## enum field declaration
    FldU = (60, "fld")  ## field declaration
    ElifU = (78, "elif")  ## pair of (condition, action)
    ElseU = (79, "else")  ## `else` action
    TypevarsU = (80, "typevars")  ## type variable/generic parameters
    OfU = (86, "of")  ## `of` branch within a `case` statement
    PragmasU = (126, "pragmas")  ## begin of pragma section
    UnpackflatU = (154, "unpackflat")  ## unpack into flat variable list
    UnpacktupU = (155, "unpacktup")  ## unpack tuple
    ExceptU = (157, "except")  ## except subsection
    FinU = (158, "fin")  ## finally subsection

proc rawTagIsNimonyOther*(raw: uint32): bool {.inline.} =
  let r = raw - 27'u32
  r <= 255'u32 and r.uint8 in {0'u8, 1'u8, 21'u8, 22'u8, 26'u8, 31'u8, 32'u8, 33'u8, 51'u8, 52'u8, 53'u8, 59'u8, 99'u8, 127'u8, 128'u8, 130'u8, 131'u8}

type
  NimonyPragma* = enum
    NoPragma
    EmitP = (73, "emit")  ## emit statement
    InlineP = (120, "inline")  ## `inline` proc annotation
    NoinlineP = (121, "noinline")  ## `noinline` proc annotation
    VarargsP = (123, "varargs")  ## `varargs` proc annotation
    SelectanyP = (125, "selectany")
    AlignP = (128, "align")
    BitsP = (129, "bits")
    NodeclP = (132, "nodecl")  ## `nodecl` annotation
    RaisesP = (147, "raises")  ## proc annotation
    MagicP = (185, "magic")  ## `magic` pragma
    ImportcP = (186, "importc")  ## `importc` pragma
    ImportcppP = (187, "importcpp")  ## `importcpp` pragma
    ExportcP = (188, "exportc")  ## `exportc` pragma
    HeaderP = (189, "header")  ## `header` pragma
    ThreadvarP = (190, "threadvar")  ## `threadvar` pragma
    GlobalP = (191, "global")  ## `global` pragma
    DiscardableP = (192, "discardable")  ## `discardable` pragma
    NoreturnP = (193, "noreturn")  ## `noreturn` pragma
    BorrowP = (194, "borrow")  ## `borrow` pragma
    NoSideEffectP = (195, "noSideEffect")  ## `noSideEffect` pragma
    NodestroyP = (196, "nodestroy")  ## `nodestroy` pragma
    PluginP = (197, "plugin")  ## `plugin` pragma
    BycopyP = (198, "bycopy")  ## `bycopy` pragma
    ByrefP = (199, "byref")  ## `byref` pragma
    NoinitP = (200, "noinit")  ## `noinit` pragma
    RequiresP = (201, "requires")  ## `requires` pragma
    EnsuresP = (202, "ensures")  ## `ensures` pragma
    BuildP = (203, "build")  ## `build` pragma
    StringP = (204, "string")  ## `string` pragma

proc rawTagIsNimonyPragma*(raw: uint32): bool {.inline.} =
  let r = raw - 73'u32
  r <= 255'u32 and r.uint8 in {0'u8, 47'u8, 48'u8, 50'u8, 52'u8, 55'u8, 56'u8, 59'u8, 74'u8, 112'u8, 113'u8, 114'u8, 115'u8, 116'u8, 117'u8, 118'u8, 119'u8, 120'u8, 121'u8, 122'u8, 123'u8, 124'u8, 125'u8, 126'u8, 127'u8, 128'u8, 129'u8, 130'u8, 131'u8}

type
  NimonySym* = enum
    NoSym
    VarY = (52, "var")  ## variable declaration
    ParamY = (53, "param")  ## parameter declaration
    ConstY = (54, "const")  ## const variable declaration
    ResultY = (55, "result")  ## result variable declaration
    LetY = (56, "let")  ## let variable declaration
    CursorY = (57, "cursor")  ## cursor variable declaration
    TypevarY = (58, "typevar")  ## type variable declaration
    EfldY = (59, "efld")  ## enum field declaration
    FldY = (60, "fld")  ## field declaration
    ProcY = (61, "proc")  ## proc declaration
    FuncY = (62, "func")  ## function declaration
    IteratorY = (63, "iterator")  ## iterator declaration
    ConverterY = (64, "converter")  ## converter declaration
    MethodY = (65, "method")  ## method declaration
    MacroY = (66, "macro")  ## macro declaration
    TemplateY = (67, "template")  ## template declaration
    TypeY = (68, "type")  ## type declaration
    BlockY = (69, "block")  ## block declaration
    ModuleY = (70, "module")  ## module declaration
    CchoiceY = (71, "cchoice")  ## closed choice

proc rawTagIsNimonySym*(raw: uint32): bool {.inline.} =
  raw >= 52'u32 and raw <= 71'u32

type
  ControlFlowKind* = enum
    NoControlFlow
    IteF = (150, "ite")  ## if-then-else
    GraphF = (151, "graph")  ## disjoint subgraph annotation
    ForbindF = (152, "forbind")  ## bindings for a `for` loop but the loop itself is mapped to gotos
    KillF = (153, "kill")  ## some.var is about to disappear (scope exit)

proc rawTagIsControlFlowKind*(raw: uint32): bool {.inline.} =
  raw >= 150'u32 and raw <= 153'u32

