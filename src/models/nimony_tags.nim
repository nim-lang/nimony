# Generated by tools/gen_tags.nim from doc/tags.md. DO NOT EDIT!


type
  NimonyExpr* = enum
    NoExpr
    ErrX = (1, "err")  ## indicates an error
    SufX = (2, "suf")  ## literal with suffix annotation
    AtX = (3, "at")  ## array indexing operation
    DerefX = (4, "deref")  ## pointer deref operation
    DotX = (5, "dot")  ## object field selection
    PatX = (6, "pat")  ## pointer indexing operation
    ParX = (7, "par")  ## syntactic parenthesis
    AddrX = (8, "addr")  ## address of operation
    NilX = (9, "nil")  ## nil pointer value
    InfX = (10, "inf")  ## positive infinity floating point value
    NeginfX = (11, "neginf")  ## negative infinity floating point value
    NanX = (12, "nan")  ## NaN floating point value
    FalseX = (13, "false")  ## boolean `false` value
    TrueX = (14, "true")  ## boolean `true` value
    AndX = (15, "and")  ## boolean `and` operation
    OrX = (16, "or")  ## boolean `or` operation
    NotX = (17, "not")  ## boolean `not` operation
    NegX = (18, "neg")  ## negation operation
    SizeofX = (19, "sizeof")  ## `sizeof` operation
    AlignofX = (20, "alignof")  ## `alignof` operation
    OffsetofX = (21, "offsetof")  ## `offsetof` operation
    OconstrX = (22, "oconstr")  ## object constructor
    AconstrX = (23, "aconstr")  ## array constructor
    AddX = (25, "add")
    SubX = (26, "sub")
    MulX = (27, "mul")
    DivX = (28, "div")
    ModX = (29, "mod")
    ShrX = (30, "shr")
    ShlX = (31, "shl")
    BitandX = (32, "bitand")
    BitorX = (33, "bitor")
    BitxorX = (34, "bitxor")
    BitnotX = (35, "bitnot")
    EqX = (36, "eq")
    NeqX = (37, "neq")
    LeX = (38, "le")
    LtX = (39, "lt")
    CastX = (40, "cast")
    ConvX = (41, "conv")  ## type conversion
    CallX = (42, "call")  ## call operation
    CmdX = (43, "cmd")  ## command operation
    CchoiceX = (67, "cchoice")  ## closed choice
    OchoiceX = (68, "ochoice")  ## open choice
    QuotedX = (195, "quoted")  ## name in backticks
    HderefX = (196, "hderef")  ## hidden pointer deref operation
    DdotX = (197, "ddot")  ## deref dot
    HaddrX = (198, "haddr")  ## hidden address of operation
    NewoconstrX = (199, "newoconstr")  ## new object constructor
    TupleconstrX = (200, "tupleconstr")  ## tuple constructor
    SetX = (201, "set")  ## set constructor
    AshrX = (202, "ashr")
    OconvX = (203, "oconv")  ## object conversion
    HconvX = (204, "hconv")  ## hidden basic type conversion
    DconvX = (205, "dconv")  ## conversion between `distinct` types
    CallstrlitX = (206, "callstrlit")
    InfixX = (207, "infix")
    PrefixX = (208, "prefix")
    HcallX = (209, "hcall")  ## hidden converter call
    CompilesX = (210, "compiles")
    DeclaredX = (211, "declared")
    DefinedX = (212, "defined")
    HighX = (213, "high")
    LowX = (214, "low")
    TypeofX = (215, "typeof")
    UnpackX = (216, "unpack")
    EnumtostrX = (217, "enumtostr")
    IsmainmoduleX = (218, "ismainmodule")
    DefaultobjX = (219, "defaultobj")
    DefaulttupX = (220, "defaulttup")
    ExprX = (221, "expr")
    ArratX = (222, "arrat")
    TupatX = (223, "tupat")
    PlussetX = (224, "plusset")
    MinussetX = (225, "minusset")
    MulsetX = (226, "mulset")
    XorsetX = (227, "xorset")
    EqsetX = (228, "eqset")
    LesetX = (229, "leset")
    LtsetX = (230, "ltset")
    InsetX = (231, "inset")
    CardX = (232, "card")
    EmoveX = (233, "emove")
    DestroyX = (234, "destroy")
    DupX = (235, "dup")
    CopyX = (236, "copy")
    WasmovedX = (237, "wasmoved")
    SinkhX = (238, "sinkh")
    TraceX = (239, "trace")

proc rawTagIsNimonyExpr*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {1'u8, 2'u8, 3'u8, 4'u8, 5'u8, 6'u8, 7'u8, 8'u8, 9'u8, 10'u8, 11'u8, 12'u8, 13'u8, 14'u8, 15'u8, 16'u8, 17'u8, 18'u8, 19'u8, 20'u8, 21'u8, 22'u8, 23'u8, 25'u8, 26'u8, 27'u8, 28'u8, 29'u8, 30'u8, 31'u8, 32'u8, 33'u8, 34'u8, 35'u8, 36'u8, 37'u8, 38'u8, 39'u8, 40'u8, 41'u8, 42'u8, 43'u8, 67'u8, 68'u8, 195'u8, 196'u8, 197'u8, 198'u8, 199'u8, 200'u8, 201'u8, 202'u8, 203'u8, 204'u8, 205'u8, 206'u8, 207'u8, 208'u8, 209'u8, 210'u8, 211'u8, 212'u8, 213'u8, 214'u8, 215'u8, 216'u8, 217'u8, 218'u8, 219'u8, 220'u8, 221'u8, 222'u8, 223'u8, 224'u8, 225'u8, 226'u8, 227'u8, 228'u8, 229'u8, 230'u8, 231'u8, 232'u8, 233'u8, 234'u8, 235'u8, 236'u8, 237'u8, 238'u8, 239'u8}

type
  NimonyStmt* = enum
    NoStmt
    CallS = (42, "call")  ## call operation
    CmdS = (43, "cmd")  ## command operation
    VarS = (48, "var")  ## variable declaration
    ConstS = (50, "const")  ## const variable declaration
    ResultS = (51, "result")  ## result variable declaration
    LetS = (52, "let")  ## let variable declaration
    CursorS = (53, "cursor")  ## cursor variable declaration
    ProcS = (57, "proc")  ## proc declaration
    FuncS = (58, "func")  ## function declaration
    IteratorS = (59, "iterator")  ## iterator declaration
    ConverterS = (60, "converter")  ## converter declaration
    MethodS = (61, "method")  ## method declaration
    MacroS = (62, "macro")  ## macro declaration
    TemplateS = (63, "template")  ## template declaration
    TypeS = (64, "type")  ## type declaration
    BlockS = (65, "block")  ## block declaration
    EmitS = (69, "emit")  ## emit statement
    AsgnS = (70, "asgn")  ## assignment statement
    ScopeS = (71, "scope")  ## explicit scope annotation, like `stmts`
    IfS = (72, "if")  ## if statement header
    WhenS = (73, "when")  ## when statement header
    BreakS = (77, "break")  ## `break` statement
    ContinueS = (78, "continue")  ## `continue` statement
    ForS = (79, "for")  ## for statement
    WhileS = (80, "while")  ## `while` statement
    CaseS = (81, "case")  ## `case` statement
    RetS = (85, "ret")  ## `return` instruction
    YldS = (86, "yld")  ## yield statement
    StmtsS = (87, "stmts")  ## list of statements
    PragmasS = (120, "pragmas")  ## begin of pragma section
    InclS = (126, "incl")  ## `#include` statement or `incl` set operation
    ExclS = (127, "excl")  ## `excl` set operation
    IncludeS = (128, "include")  ## `include` statement
    ImportS = (129, "import")  ## `import` statement
    FromS = (130, "from")  ## `from` statement
    ImportexceptS = (131, "importexcept")  ## `importexcept` statement
    ExportS = (132, "export")  ## `export` statement
    CommentS = (133, "comment")  ## `comment` statement
    DiscardS = (134, "discard")  ## `discard` statement
    TryS = (135, "try")  ## `try` statement
    RaiseS = (136, "raise")  ## `raise` statement

proc rawTagIsNimonyStmt*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {42'u8, 43'u8, 48'u8, 50'u8, 51'u8, 52'u8, 53'u8, 57'u8, 58'u8, 59'u8, 60'u8, 61'u8, 62'u8, 63'u8, 64'u8, 65'u8, 69'u8, 70'u8, 71'u8, 72'u8, 73'u8, 77'u8, 78'u8, 79'u8, 80'u8, 81'u8, 85'u8, 86'u8, 87'u8, 120'u8, 126'u8, 127'u8, 128'u8, 129'u8, 130'u8, 131'u8, 132'u8, 133'u8, 134'u8, 135'u8, 136'u8}

type
  NimonyType* = enum
    NoType
    ErrT = (1, "err")  ## indicates an error
    AtT = (3, "at")  ## array indexing operation
    AndT = (15, "and")  ## boolean `and` operation
    OrT = (16, "or")  ## boolean `or` operation
    NotT = (17, "not")  ## boolean `not` operation
    IteratorT = (59, "iterator")  ## iterator declaration
    ParamsT = (88, "params")  ## list of proc parameters, also used as a "proc type"
    ObjectT = (90, "object")  ## object type declaration
    EnumT = (91, "enum")  ## enum type declaration
    ProctypeT = (92, "proctype")  ## proc type declaration (soon obsolete, use params instead)
    IT = (96, "i")  ## `int` builtin type
    UT = (97, "u")  ## `uint` builtin type
    FT = (98, "f")  ## `float` builtin type
    CT = (99, "c")  ## `char` builtin type
    BoolT = (100, "bool")  ## `bool` builtin type
    VoidT = (101, "void")  ## `void` return type
    PtrT = (102, "ptr")  ## `ptr` type contructor
    ArrayT = (103, "array")  ## `array` type constructor
    VarargsT = (117, "varargs")  ## `varargs` proc annotation
    StaticT = (140, "static")  ## `static` type or annotation
    RefobjT = (149, "refobj")  ## `ref object` type
    PtrobjT = (150, "ptrobj")  ## `ptr object` type
    TupleT = (151, "tuple")  ## `tuple` type
    OnumT = (152, "onum")  ## enum with holes type
    RefT = (153, "ref")  ## `ref` type
    MutT = (154, "mut")  ## `mut` type
    OutT = (155, "out")  ## `out` type
    LentT = (156, "lent")  ## `lent` type
    SinkT = (157, "sink")  ## `sink` type
    NiltT = (158, "nilt")  ## `nilt` type
    ConceptT = (159, "concept")  ## `concept` type
    DistinctT = (160, "distinct")  ## `distinct` type
    ItertypeT = (161, "itertype")  ## `itertype` type
    RangetypeT = (162, "rangetype")  ## `rangetype` type
    UarrayT = (163, "uarray")  ## `uarray` type
    OpenarrayT = (164, "openarray")  ## `openarray` type
    SettT = (165, "sett")  ## `sett` type
    AutoT = (166, "auto")  ## `auto` type
    SymkindT = (167, "symkind")  ## `symkind` type
    TypekindT = (168, "typekind")  ## `typekind` type
    TypedescT = (169, "typedesc")  ## `typedesc` type
    UntypedT = (170, "untyped")  ## `untyped` type
    TypedT = (171, "typed")  ## `typed` type
    CstringT = (172, "cstring")  ## `cstring` type
    PointerT = (173, "pointer")  ## `pointer` type
    OrdinalT = (174, "ordinal")  ## `ordinal` type

proc rawTagIsNimonyType*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {1'u8, 3'u8, 15'u8, 16'u8, 17'u8, 59'u8, 88'u8, 90'u8, 91'u8, 92'u8, 96'u8, 97'u8, 98'u8, 99'u8, 100'u8, 101'u8, 102'u8, 103'u8, 117'u8, 140'u8, 149'u8, 150'u8, 151'u8, 152'u8, 153'u8, 154'u8, 155'u8, 156'u8, 157'u8, 158'u8, 159'u8, 160'u8, 161'u8, 162'u8, 163'u8, 164'u8, 165'u8, 166'u8, 167'u8, 168'u8, 169'u8, 170'u8, 171'u8, 172'u8, 173'u8, 174'u8}

type
  NimonyOther* = enum
    NoSub
    KvU = (24, "kv")  ## key-value pair
    RangeU = (44, "range")  ## `(range a b)` construct
    RangesU = (45, "ranges")
    ParamU = (49, "param")  ## parameter declaration
    TypevarU = (54, "typevar")  ## type variable declaration
    EfldU = (55, "efld")  ## enum field declaration
    FldU = (56, "fld")  ## field declaration
    ElifU = (74, "elif")  ## pair of (condition, action)
    ElseU = (75, "else")  ## `else` action
    TypevarsU = (76, "typevars")  ## type variable/generic parameters
    OfU = (82, "of")  ## `of` branch within a `case` statement
    PragmasU = (120, "pragmas")  ## begin of pragma section
    UnpackflatU = (145, "unpackflat")  ## unpack into flat variable list
    UnpacktupU = (146, "unpacktup")  ## unpack tuple
    ExceptU = (147, "except")  ## except subsection
    FinU = (148, "fin")  ## finally subsection

proc rawTagIsNimonyOther*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {24'u8, 44'u8, 45'u8, 49'u8, 54'u8, 55'u8, 56'u8, 74'u8, 75'u8, 76'u8, 82'u8, 120'u8, 145'u8, 146'u8, 147'u8, 148'u8}

type
  NimonyPragma* = enum
    NoPragma
    EmitP = (69, "emit")  ## emit statement
    InlineP = (114, "inline")  ## `inline` proc annotation
    NoinlineP = (115, "noinline")  ## `noinline` proc annotation
    VarargsP = (117, "varargs")  ## `varargs` proc annotation
    SelectanyP = (119, "selectany")
    AlignP = (121, "align")
    BitsP = (122, "bits")
    NodeclP = (125, "nodecl")  ## `nodecl` annotation
    RaisesP = (138, "raises")  ## proc annotation
    MagicP = (175, "magic")  ## `magic` pragma
    ImportcP = (176, "importc")  ## `importc` pragma
    ImportcppP = (177, "importcpp")  ## `importcpp` pragma
    ExportcP = (178, "exportc")  ## `exportc` pragma
    HeaderP = (179, "header")  ## `header` pragma
    ThreadvarP = (180, "threadvar")  ## `threadvar` pragma
    GlobalP = (181, "global")  ## `global` pragma
    DiscardableP = (182, "discardable")  ## `discardable` pragma
    NoreturnP = (183, "noreturn")  ## `noreturn` pragma
    BorrowP = (184, "borrow")  ## `borrow` pragma
    NoSideEffectP = (185, "noSideEffect")  ## `noSideEffect` pragma
    NodestroyP = (186, "nodestroy")  ## `nodestroy` pragma
    PluginP = (187, "plugin")  ## `plugin` pragma
    BycopyP = (188, "bycopy")  ## `bycopy` pragma
    ByrefP = (189, "byref")  ## `byref` pragma
    NoinitP = (190, "noinit")  ## `noinit` pragma
    RequiresP = (191, "requires")  ## `requires` pragma
    EnsuresP = (192, "ensures")  ## `ensures` pragma
    BuildP = (193, "build")  ## `build` pragma
    StringP = (194, "string")  ## `string` pragma

proc rawTagIsNimonyPragma*(raw: uint32): bool =
  raw <= 255'u32 and raw.uint8 in {69'u8, 114'u8, 115'u8, 117'u8, 119'u8, 121'u8, 122'u8, 125'u8, 138'u8, 175'u8, 176'u8, 177'u8, 178'u8, 179'u8, 180'u8, 181'u8, 182'u8, 183'u8, 184'u8, 185'u8, 186'u8, 187'u8, 188'u8, 189'u8, 190'u8, 191'u8, 192'u8, 193'u8, 194'u8}

type
  NimonySym* = enum
    NoSym
    VarY = (48, "var")  ## variable declaration
    ParamY = (49, "param")  ## parameter declaration
    ConstY = (50, "const")  ## const variable declaration
    ResultY = (51, "result")  ## result variable declaration
    LetY = (52, "let")  ## let variable declaration
    CursorY = (53, "cursor")  ## cursor variable declaration
    TypevarY = (54, "typevar")  ## type variable declaration
    EfldY = (55, "efld")  ## enum field declaration
    FldY = (56, "fld")  ## field declaration
    ProcY = (57, "proc")  ## proc declaration
    FuncY = (58, "func")  ## function declaration
    IteratorY = (59, "iterator")  ## iterator declaration
    ConverterY = (60, "converter")  ## converter declaration
    MethodY = (61, "method")  ## method declaration
    MacroY = (62, "macro")  ## macro declaration
    TemplateY = (63, "template")  ## template declaration
    TypeY = (64, "type")  ## type declaration
    BlockY = (65, "block")  ## block declaration
    ModuleY = (66, "module")  ## module declaration
    CchoiceY = (67, "cchoice")  ## closed choice

proc rawTagIsNimonySym*(raw: uint32): bool =
  raw >= 48'u32 and raw <= 67'u32

type
  ControlFlowKind* = enum
    NoControlFlow
    IteF = (141, "ite")  ## if-then-else
    GraphF = (142, "graph")  ## disjoint subgraph annotation
    ForbindF = (143, "forbind")  ## bindings for a `for` loop but the loop itself is mapped to gotos
    KillF = (144, "kill")  ## some.var is about to disappear (scope exit)

proc rawTagIsControlFlowKind*(raw: uint32): bool =
  raw >= 141'u32 and raw <= 144'u32

