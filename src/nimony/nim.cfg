--path: "../lib"
--warning[UnusedImport]=off
# Btw this relies on a Nim bug where it interprets the --outdir relative to the current directory
# instead of the directory of this nim.cfg file.
--outdir: "bin"
# nimsem/nimony run on the in-memory virtual-ParRi token model
# (sealed jump fields + ParRi elision), like lengc:
--define: virtualParRi
